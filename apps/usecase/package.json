{
  "name": "@recipeaceful/usecase",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "watch": "tsc -w",
    "build": "npx tsc -p tsconfig.json && npx tsc-alias -p tsconfig.json",
    "lint:fix": "eslint --fix **/*.{ts,d.ts,js,cjs,mjs}"
  },
  "devDependencies": {
    "@recipeaceful/library": "^1.0.0",
    "@recipeaceful/domain": "^1.0.0",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/eslint": "^8.56.1",
    "@types/node": "^20.10.6",
    "eslint": "^8.56.0",
    "tsc-alias": "1.8.8",
    "typescript": "^5.3.3"
  }
}
