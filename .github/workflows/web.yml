name: web
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v3
        with:
          node-version: v19.1.0
      - run: npm install -g pnpm
      - run: pnpm add -w -g turbo
      - run: pnpm install
      - run: turbo build
      - run: pnpm --filter web run lint
